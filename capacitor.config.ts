import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.braj.consultancy',
  appName: 'BRAJ Consultancy',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
};

export default config;
